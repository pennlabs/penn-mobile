{{- if .Values.ingress.enabled -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
{{- if .Values.staging }}
  namespace: staging
{{- end }}
spec:
  rules:
  - host: {{ if .Values.staging }}staging.{{ end }}{{ .Values.ingress.domain }}
    http:
      paths:
      - path: /
        backend:
          serviceName: {{ .Release.Name }}-svc
          servicePort: 80
{{- end }}
